/** A dashed "perforated" divider between sections — part of the shipping-manifest visual motif. */
export default function Perf() {
  return (
    <div className="wrap">
      <div className="perf" />
    </div>
  )
}
